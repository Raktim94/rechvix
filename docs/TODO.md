# Build TODO

Master task list for the whole project, staged per `docs/architecture.md`
§16. Checked items are actually built, tested, and merged — not just
started. This file is the source of truth for "what's done"; update it as
part of finishing a stage, not after the fact.

## Stage 0 — Research ✅ (2026-09-02)
- [x] Verify Go/PostgreSQL current versions
- [x] Verify current GST e-Invoice/e-Way Bill API schema state
- [x] `docs/research.md` produced

## Stage 1 — Architecture ✅ (2026-09-02)
- [x] Module diagram, ERD shape, layering rules
- [x] Tax/inventory/accounting/GST/e-Invoice/e-Way Bill architecture
- [x] Threat model, security architecture
- [x] Docker/CasaOS/Tauri plan (no bundled reverse proxy, reasoning documented)
- [x] `docs/architecture.md` produced, user confirmed ("ok do it")

## Stage 2 — Foundation ✅ (2026-09-02)
- [x] `internal/platform/{config,logging,database,observability,http,crypto,permissions,audit,money}`
- [x] Migrations: organisations → warehouses, RBAC catalog, users, sessions, MFA, password reset, audit_log — RLS on every tenant table
- [x] `internal/modules/identity` — login, sessions, TOTP MFA + recovery codes, password change/reset, bootstrap
- [x] `internal/modules/organisation` — org/legal-entity/branch/warehouse CRUD
- [x] `apps/server` composition root
- [x] ADR: Argon2id parameters (benchmarked)
- [x] Unit tests (46/46) + Testcontainers integration tests incl. RLS cross-tenant isolation (6/6), independently re-verified
- [x] `docs/adr/0001-argon2id-parameters.md`

## Stage 3 — Catalogue / Contacts / Pricing ✅ mostly (2026-09-02, one carry-over)
- [x] `internal/modules/catalogue`: products, product_variants, SKUs, barcodes, categories, brands
- [x] Units of measure + explicit unit_conversions (BOX→PCS, etc.), auditable
- [x] `internal/modules/contacts`: parties (customer/supplier/both), addresses (billing/shipping/warehouse/registered), tax registrations, credit limit, payment terms
- [x] `internal/modules/pricing`: price lists, price list items, multi-currency price fields (uses `internal/platform/money`)
- [x] GST fields on catalogue (HSN/SAC) and contacts (GSTIN, state code) — data only, no tax calculation logic yet (that's Stage 5/`gstindia`)
- [x] Global search groundwork: trigram indexes on product name/SKU (contacts trigram search: verify alongside the carry-over item below)
- [ ] **Carried to Stage 4:** import/export scaffolding for products/customers/suppliers (CSV/XLSX, dry-run + validation report, brief §53) — not built in the Stage 3 pass, genuinely missing, not deferred by decision
- [x] Unit tests (63/63) + integration tests (15/15 incl. per-module RLS cross-org checks), independently re-verified

## Stage 4 — Inventory ✅ (2026-09-03, one scope note)
- [x] `internal/modules/inventory`: stock_movements (append-only), stock_balances (materialized, app-layer-maintained — see `docs/adr/0002-stock-balance-maintenance.md`)
- [x] Movement types per brief §11 (OPENING, PURCHASE_RECEIPT, PURCHASE_RETURN, SALE, SALE_RETURN, TRANSFER_IN/OUT, ADJUSTMENT_IN/OUT, ASSEMBLY_IN/OUT, DAMAGE, EXPIRY) — SALE/SALE_RETURN/ASSEMBLY_* wired as movement-recording primitives only; the sales/assembly documents that call them are Stage 5+ scope
- [x] Batches (batch+lot modeled as one concept, see migration comment), serial numbers, expiry/manufacturing dates
- [x] Stock reservations, low stock/reorder/safety stock (`stock_policies`)
- [x] Weighted-average costing strategy (unit-conversion-normalized — real bug caught and fixed in dev, see `docs/adr/0002-stock-balance-maintenance.md` and `TestInventory_UnitConversionAwareReceipt`)
- [x] `internal/modules/purchases`: purchase order, goods receipt (GRN — posts stock), purchase invoice, purchase return (reverses stock), debit note. **Supplier payment/credit deliberately NOT built** — needs Stage 6's ledger/payment infrastructure to post against; a payment table with nothing to apply it to would just be a stub. Permissions reuse Stage 2's pre-seeded `purchase.*` codes (not a new `purchases.*` namespace)
- [x] Concurrency tests: last-unit oversell race (only one of 8 concurrent takers wins), concurrent multi-warehouse transfer (no lost/duplicated stock), concurrent document-number allocation (Scenario D/I), all re-run multiple times for stability
- [x] Unit tests (78/78) + integration tests (39/39, incl. RLS sweep across every new table) — independently re-verified
- [x] **Carried-over CSV/XLSX import from Stage 3, completed this stage**: `internal/platform/importer` (shared parse + dry-run/report scaffolding) wired into `catalogue.ImportProducts` and `contacts.ImportParties`, with dedup + per-row validation reporting

## Stage 5a — Tax Engine ✅ (2026-09-03)
- [x] `internal/modules/taxation`: generic `TaxEngine` interface, `Money`-based calculation, tax_document/tax_line/tax_component model, tax_rate_master with valid_from/valid_to
- [x] `internal/modules/gstindia`: `IndiaGSTEngine` — GSTIN, place of supply, intra/inter-state CGST+SGST/UTGST vs IGST, cess, HSN, exempt/nil-rated/zero-rated, B2B/B2C/export/SEZ carried on tax_document; reverse-charge as a caller-set flag (liability accounting is Stage 6)
- [x] Golden tax fixtures: 0/3/5/12/18/28/40%, ₹90 inclusive @18% fixture (verified against the brief's exact 76.271186.../13.728813... figures), intra-state CGST+SGST split, inter-state IGST, cess-on-taxable-not-gross, UTGST, valid_from/valid_to snapshot immutability — independently re-verified, numbers checked by hand against the actual test code, not just trusted
- [x] Unit tests (96/96) + integration tests (47/47) — independently re-verified, 2 real bugs caught by tests and fixed (RLS-scope-before-calculation, CHECK-constraint default)

## Stage 5b — Sales Documents & Printing ✅ (2026-09-03)
- [x] `internal/modules/sales`: quotation, proforma, sales order, delivery challan, tax invoice, POS invoice, credit/debit note, sales return, recurring invoice (document_type-parameterized, one family); `ConvertDocument` generically covers quotation/sales-order/challan → invoice
- [x] Invoice header + line fields per brief §5, calculation snapshot immutability (brief §55, tested against a live tax-rate-master change) — calls Stage 5a's `TaxEngine` via new `taxation.CalculateAndSnapshotTx`, calls `inventory.RecordMovementForOtherModule` on finalize, all inside one transaction
- [x] Document numbering: `internal/platform/numbering`, configurable series scoped to organisation/branch/document-type/financial-year (brief §51), concurrency-safe allocation verified under real concurrent load (Scenario I) — the real system purchases' Stage 4 counter was a placeholder for; purchases itself not yet migrated onto it (follow-up)
- [x] Printing/PDF template engine (`internal/modules/sales/printing`, go-pdf/fpdf): A4 GST invoice, compact, thermal 80mm/58mm, quotation, PO, challan, receipt, statement, credit/debit note (brief §19) — "Previous Balance" field intentionally stubbed nil until Stage 6's ledger exists
- [x] Sales screen API groundwork: `BillingLookup` (product search + stock + price in one call, brief §24/§25)
- [x] Unit tests (100/100) + integration tests (53/53, incl. finalize atomicity, numbering concurrency, tax-snapshot immutability, RLS, PDF rendering) — independently re-verified, one real bug caught by the print test and fixed (RLS-scope-before-read ordering, same class as earlier stages)

## Stage 6 — Accounting ✅ (2026-09-03, one scope note)
- [x] `internal/modules/accounting`: chart_of_accounts (idempotent default-seed via `EnsureDefaultChartOfAccounts`), journals, journal_lines, fiscal_periods, receipts, payments, bank_accounts, reconciliations
- [x] Double-entry invariant enforced at 3 layers per `docs/architecture.md` §7 — app-layer `validateBalanced` (unit + property tested), a Postgres DEFERRED CONSTRAINT TRIGGER re-verifying Σdebit=Σcredit at COMMIT (proved via raw SQL bypassing the Go app layer entirely), and an unconditional BEFORE UPDATE/DELETE trigger making journal_lines immutable from the instant they're written (also proved via raw SQL) — a trigger rather than GRANT/REVOKE specifically because a table owner bypasses grants the same way it bypasses RLS (Stage 2's lesson), so a trigger is the one mechanism guaranteed to hold regardless of which role connects
- [x] Auto-posting from finalized sales/purchase documents (`docs/adr/0003-accounting-integration-point.md`) — sales posts Dr AR/Cr Sales+Tax Payable (or reversed for credit notes/returns) using the real tax snapshot; purchases posts Dr Purchases/Cr AP for invoice/return/debit-note. **Scope note, not silently skipped:** purchases posts a single line-total amount, not split into a separate GST Input Tax Credit line — purchases has never been wired through the tax engine (unlike sales), and doing that properly is flagged as a bounded, well-precedented follow-up in the ADR rather than rushed here
- [x] Customer/supplier ledger (`GetPartyLedger`) — derived fresh from journal_lines every call, never a mutable stored balance column (explicitly avoiding nodedr-pos's real Float-`increment` balance-drift incident); ageing (`GetAgeing`) via oldest-first FIFO credit-to-debit matching
- [x] Fiscal year config + period locking (brief §52) — `accounting.override_locked_period` is a separate permission from `accounting.post`; lock lifecycle tested, override-path tested (the bootstrap Owner holds every permission so the "non-override user is blocked" direction isn't separately covered — flagged, not silently assumed)
- [x] Property test: Σdebit == Σcredit for any valid journal (200 randomized generate-then-check iterations, plus a perturbation check that a 1-paisa-unbalanced journal is always rejected)
- [x] Scenario E passing end-to-end: ₹10,000 tax-inclusive credit sale → ₹4,000 receipt → ledger shows exactly ₹6,000 outstanding
- [x] Unit tests (105/105) + integration tests (62/62, incl. RLS on journals/journal_lines via raw SQL with no org filter in the query — the RLS-itself test, not an app-layer WHERE-clause test) — independently re-verified, one real bug caught by the test suite and fixed (migration declared `NOT NULL DEFAULT ''` on `description`/`reference_number` columns, inconsistent with the nullable+`nullIfEmpty`/`COALESCE` convention every other module in this codebase uses — fixed in the migration before it was ever pushed)

## Stage 7 — Reports / Dashboard ✅ (2026-09-03, two scope notes)
- [x] Sales: summary (day/month/customer/product/category/salesperson/branch/warehouse), invoice detail, gross profit (**approximate COGS** — current `average_cost`, not historical-at-sale-time; see `domain.GrossProfitRow`'s doc comment and follow-up note below)
- [x] Purchases: summary (by supplier/product/etc.), document detail — no taxable/tax breakdown yet (purchases still isn't wired through the tax engine, a known gap flagged since Stage 4/6)
- [x] Inventory: stock valuation, low stock, stock movements
- [x] Accounting: trial balance, org-wide receivables/payables (batched over Stage 6's existing per-party ageing), account ledger (cash/bank book)
- [x] Tax: HSN summary, tax-rate summary, GSTR-1-oriented preparation (explicitly labeled NOT a filing submission in both the API response and UI-facing title string) — **GSTR-3B-oriented summary deliberately NOT built**: its inward/ITC side needs purchases' tax-engine wiring, which doesn't exist; building only the outward half risked presenting an incomplete GSTR-3B as if complete, so it's deferred as a real gap rather than shipped half-true
- [x] Export: CSV/XLSX/JSON/PDF via one shared `internal/platform/export` writer, `?format=` query param on every report endpoint — synchronous only; the background-job + expiring-link half of brief §54 needs `apps/worker`/an outbox mechanism that doesn't exist until Stage 9, documented as a follow-up rather than faked
- [x] Dashboard: all 8 cards, live indexed queries per `docs/adr/0004-dashboard-query-design.md` (not a materialized table — reasoning + a measured sanity check at ~500+200 documents documented there)
- [x] Shared filter-building helper (`internal/modules/reporting/pg`'s `whereBuilder`) + a validated `GroupDimension` allow-list for GROUP BY (never raw-string SQL, brief §62)
- [x] Unit tests (10 new) + integration tests (11 new, incl. a report-specific cross-organisation RLS-leak test across 5 report types and a dashboard performance sanity check) — independently re-verified, one real bug caught and fixed (ambiguous `organisation_id` column reference on every multi-table-join report query — ~10 query sites)

## Stage 8 — Government Integrations (sandbox only) ✅ (2026-09-03, follow-ups noted)
- [x] `internal/platform/outbox`: generic transactional outbox (`outbox_events`, a SECURITY DEFINER `outbox_claim_next()` for the one deliberate cross-org RLS bypass a background poller needs — same pattern as migrations/0003's login lookup), `Poller` with exponential backoff — this did not exist before Stage 8 and is built reusable for Stage 9, not e-Invoice-specific
- [x] `apps/worker`: real, separate composition-root process running the outbox poller — never inline with the HTTP request path
- [x] `internal/modules/einvoice`: `EInvoiceProvider` interface (exact signature from `docs/architecture.md` §9), `v1/mock` (canned, deterministic, zero network calls — the only provider automated tests ever exercise) and `v1/sandbox` (real NIC-sandbox-calling adapter, wired into `apps/worker` behind `EINVOICE_PROVIDER=sandbox`, reviewed-by-reading not proven-by-testing — brief Rule 17), persists IRN/ack/signed QR/status/correlation id per §9's field list
- [x] `internal/modules/ewaybill`: generate/retrieve/cancel/Part-B history/voluntary closure — **Ship-to GSTIN (URP sentinel) + CLOSED status distinct from CANCELLED** (2026-08-01 GSTN change, `docs/research.md`) present from the first migration, round-trip tested
- [x] Explicit persisted status machine (DRAFT→QUEUED→SUBMITTING→GENERATED|FAILED_RETRYABLE|FAILED_FINAL, CANCEL_PENDING→CANCELLED, CLOSED) — real CHECK-constrained columns, not inferred
- [x] Never called from sales domain code directly — `sales.FinalizeDocument` only enqueues an outbox event (one INSERT, same transaction as finalize); the government API call happens later, in `apps/worker`, never inline with the HTTP request (`docs/architecture.md` §9, brief Rule 12, Scenario L)
- [x] Idempotent: `einvoice_records.sales_document_id` UNIQUE constraint + an in-service Terminal-status check backs it up — a reprocessed outbox event is a safe no-op, tested explicitly (double-processing calls the provider exactly once)
- [x] Unit tests (6 new) + integration tests (6 new: full flow, failed-then-retry, idempotency, outage-doesn't-corrupt-sale, RLS, Ship-to-GSTIN/closure round-trip) — independently re-verified, 200/200 total (was 188), zero real network calls to any government host confirmed by grep across the whole test suite
- [x] Fixture/schema round-trip tests against the mock adapter's canned shapes (IRN, ack, QR, GSTIN, EWB number/validity/Part-B); **no production or sandbox credentials used in the automated test suite** (brief Rule 17) — Scenario J/K's spirit satisfied via the mock adapter per the brief's own "use sandbox/mock adapters" instruction, not by calling the real NIC sandbox
- [ ] **Follow-ups, not done**: `einvoice_provider_credentials` (encrypted-at-rest table) exists but `apps/worker` currently sources sandbox credentials from env vars, not yet reading+decrypting per-legal-entity from that table; only `TAX_INVOICE` auto-enqueues e-Invoice generation (CREDIT_NOTE/DEBIT_NOTE e-invoicing is a real requirement, deferred); `purchases` documents aren't wired through this (only `sales`); e-Way Bill generation is a manually-triggered API call, not auto-enqueued from finalize (deliberate — no goods-movement-threshold business rule exists yet to decide *when* one is legally required, brief Rule 2)

## Stage 8c — Free-First e-Way Bill Portal Workflow (backend) ✅ (2026-09-03, real caveats below)
New requirement, not in the original brief: API access to e-Way Bill generation
must be optional — `FREE_PORTAL` mode (no paid API, prepare-and-hand-off to the
official government portal, then import the result) is the default production
mode; Stage 8's `AUTOMATIC_API` path stays as an optional accelerator with
fallback to free mode on failure. Full reasoning and constraints in
`docs/architecture.md` §9b.
- [x] `CanonicalEWayBill` model built from a finalized invoice's immutable snapshot (never live/mutable master data) — proven with a real test that edits the customer's address via raw SQL AFTER capture and confirms the stored snapshot still reflects the pre-edit value
- [x] `EWayBillOrchestrator` (`internal/modules/ewaybill/eligibility`) + versioned eligibility rule engine — threshold/rules as versioned `ewaybill_eligibility_rules` data (`valid_from`/`valid_until`, state-specific override support), never a hardcoded Go literal. **Seeded threshold (₹50,000) is a documented starting default, NOT verified against current live CBIC/GST notifications** — flagged in the migration's own INSERT comment and in `eligibility.Rule`'s doc comment; operators must confirm the applicable current rule.
- [x] `EWayBillPortalExporter` interface + `internal/modules/ewaybill/portal/v1` mapper: file-size limits with batch-splitting (tested), human-readable filenames (`EWB-<invoice-number>-<date>.json`, sanitized, tested). **The JSON field shape itself is an explicitly-labeled placeholder, NOT verified against a live official NIC bulk-upload sample** — see the prominent caveat comment at the top of `internal/modules/ewaybill/portal/v1/mapper.go`. Do not treat it as production-ready without independent verification against current government documentation.
- [x] Extended status model on the existing `ewaybill_records.status` column (NOT_REQUIRED/NEEDS_INFORMATION/READY/PREPARING/PORTAL_FILE_READY/AWAITING_PORTAL_COMPLETION/EXPIRED added; GENERATED/CANCELLED/CLOSED reused). The brief's suggested `API_QUEUED`/`API_GENERATING`/`API_NEEDS_ATTENTION` names were NOT introduced as new values — Stage 8's existing QUEUED/SUBMITTING/FAILED_RETRYABLE already serve that role and renaming verified Stage 8 code wasn't worth the risk; noted as a naming-only divergence.
- [x] `internal/modules/logistics`: `vehicles` + `transporters` (org-scoped, RLS, CRUD, tested) with a `customer_transport_preferences` smart-default resolver (recency-based)
- [x] `internal/modules/ewaybill/govportal.Service.GetOfficialEWayBillPortalURL()` — backend/env-configured only, zero parameters (structurally provable no caller can override it), tested
- [x] Import/verify flow: manual entry (the universal fallback, tested) + imported-result verification against the invoice's own snapshot (number/date/GSTIN/doc type), mismatch explicitly rejected and audited, never silently linked (tested). **PDF parsing was NOT built** — out of scope for this pass, manual entry covers the universal-fallback requirement
- [x] Hard constraints verified via grep before commit: zero Selenium/Playwright/headless-browser code or dependency, zero stored government-portal credential fields anywhere in schema or code
- [x] Audit every step (eligibility evaluated, portal upload prepared, manual result recorded, import mismatch rejected, import result linked) via the existing `internal/platform/audit` — added after an initial pass genuinely missed it, caught before commit
- [x] API-failure → free-portal fallback: same canonical data, no invoice re-entry — tested explicitly (mock provider forced to fail, free-portal path succeeds independently on the same document)
- [x] Unit tests (17 new: eligibility rule selection/versioning, portal filename/batch-splitting, govportal) + integration tests (9 new: snapshot immutability, eligibility states, prepare-upload, manual result, import mismatch rejection, import success, API fallback, vehicles RLS), all independently re-verified, 3× stable (236/236 total)
- [ ] **Not built, explicitly deferred**: PDF import parsing; bulk multi-invoice selection API/UI (the `SplitBatch` primitive exists and is tested, but no endpoint to select multiple invoices — no `apps/web` to hang it on yet); e-Way Bill closure/cancel wiring for FREE_PORTAL-mode records (Stage 8's `Close`/`Cancel` methods are still API-mode-only)

## Stage 9 — Integrations ✅ (2026-09-03, scope notes below)
- [x] `internal/modules/notifications`: EmailProvider/SMSProvider/WhatsAppProvider interfaces (no real adapter shipped — no credential-free public sandbox exists for any of the three, unlike Stage 8's NIC sandbox; interface + mock only, a documented, deliberate scope decision, not a gap); signed, expiring, revocable share links (redemption endpoint is genuinely unauthenticated per brief §21)
- [x] WhatsApp: interface only, official Business Platform shape (template message, not freeform), no Web scraping anywhere
- [x] API keys: high-entropy, shown once, hashed, scoped (brief §36's 7-scope list), revocable, optional expiry/IP restriction, last-used tracking, wired as a real alternative to session-cookie auth (`RequireAuthOrAPIKey`) via `permissions.WithAPIKeyScopeRestriction` — intersects a key's declared scopes with its owning user's real RBAC grants, never a wildcard default
- [x] `internal/modules/webhooks`: HMAC-SHA256 signed events (timestamp+eventID+body), two-hop outbox fan-out per endpoint (`docs/adr/0005`), retry/backoff via the existing outbox mechanism, dead-letter after 8 attempts with a `webhook_deliveries` visibility log — real signature cryptographically re-verified in tests, not just checked non-empty
- [x] `internal/modules/ai` + `apps/mcp`: real MCP server (official `modelcontextprotocol/go-sdk`) exposing exactly brief §39's 10 read-only tools, each calling the same permission-checked application-layer method a REST endpoint would; no tool's input schema carries an organisation_id field at all (structurally impossible to pass, not just ignored) — org scope comes only from the API key resolved once at process startup
- [x] Scenario M (MCP scope) passing end-to-end over a real in-memory MCP client/server connection — scoped key allowed on in-scope tools, rejected (real tool error, not silent empty result) on out-of-scope tools, cross-organisation document read blocked
- [x] Real bug found and fixed during this stage, worth flagging: a pre-existing architectural gap in the integration test suite — the whole test binary shares ONE physical `outbox_events` table for its entire run, and `outbox.Poller`'s cross-organisation claim (needed in production, where a worker doesn't know which org is next) let one test's poller claim and process a DIFFERENT test's queued event when run as part of the full suite. Fixed for tests via `processNextForOrg`/`drainForOrg` (`tests/integration/outbox_helpers_test.go`), which claims scoped to one already-known organisation through the same RLS every other tenant table already relies on — not a production code change, since production genuinely needs the cross-org claim. Confirmed stable across 3 full-suite runs after the fix (212/212 each time); was intermittently failing 4-8 tests before it.
- [ ] Scenario L (async resilience) — implicitly covered by the outbox pattern itself (already proven in Stage 8's outage test) but not re-asserted as a named Stage 9 test; not a gap in behavior, just not re-labeled
- [ ] Known follow-ups, not silently glossed over: per-MCP-tool-call rate limiting (brief §39) has no implementation — only identity's login-specific limiter exists in this codebase, not a general-purpose one; only `invoice.finalized`/`einvoice.generated`/`einvoice.failed` are wired webhook source events (brief §38's full catalog — invoice.created/cancelled, payment.*, stock.*, customer.created, ewaybill.* — remains unwired producer-side); MCP write tools (create_invoice_draft etc.) are explicitly out of scope per the brief itself, deferred to a later, explicitly policy-gated pass

## Stage 10a — Docker / Compose / CasaOS Packaging ✅ (2026-09-03, backend-only — see gaps below)
- [x] Multi-stage Dockerfiles: `deploy/docker/server.Dockerfile`, `deploy/docker/worker.Dockerfile` — Alpine runtime (not distroless: HEALTHCHECK needs `wget`, distroless ships no shell/HTTP client at all), non-root user, exec-form ENTRYPOINT (SIGTERM reaches the Go process directly), no frontend embed yet (`apps/web` doesn't exist) — both actually `docker build`ed successfully, not just written
- [x] `deploy/compose/docker-compose.yml`: postgres + a one-shot `migrate` service + app + worker by default, no reverse proxy (`docs/architecture.md` §12) — optional profiles `redis`/`minio` (both provisioned but genuinely unwired to anything yet, documented as such) and `reverse-proxy` (Caddy, opt-in). `docker compose config -q` passes default AND all-profiles-enabled. **Actually run end-to-end**: fresh `docker compose up -d` → all 4 containers healthy/exited-0 → `curl /health/ready` → 200 → a real `POST /api/v1/auth/bootstrap` call succeeded (201, created org/legal-entity/branch/warehouse/owner) through the full stack, proving the runtime DB role can actually read/write, not just pass a health probe → `docker compose down -v` cleaned up completely
- [x] **Real bug found and fixed by actually running this** (would have shipped broken if only reviewed, not run): Postgres 18's official image changed its expected data-volume mount point from `.../data` to a bare `/var/lib/postgresql` — the old-style mount silently never reaches a healthy state
- [x] **Real security gap found and fixed, not just documented**: the compose file originally connected the app as the same role that ran migrations — Stage 2's own `WarnIfRuntimeRoleOwnsTenantTables` logged exactly the warning it exists to catch (every RLS policy silently a no-op). Fixed properly: added a `-migrate` flag to `apps/server` (small, additive, doesn't touch existing behavior — see `apps/server/main.go`), split into a `billing_migrator` (schema owner, runs migrations only) vs. `billing_app` (runtime, RLS-respecting) role via `deploy/compose/postgres-init/01-create-runtime-role.sh` + `migrations/0029_runtime_role_grants.up.sql`. Re-verified the warning is gone in a real run.
- [x] `deploy/casaos/docker-compose.yml`: full `x-casaos` block (structure verified against nodedr-pos's real, already-submitted CasaOS manifest, not guessed), amd64+arm64, `docker compose config -q` passing
- [ ] **Gaps, not silently glossed over**: (1) CasaOS manifest references `ghcr.io/raktim94/rechvix-{server,worker}:0.1.0` images that don't exist yet — `.github/workflows/docker-publish.yml` (multi-arch, adapted from nodedr-pos's real one) is written but hasn't run against a real tag; not installable from a real CasaOS instance until it does. (3) Backup/restore automation is explicitly Stage 11 scope, not built here — `docs/operations/deployment.md` documents a manual `pg_dump` fallback and says so directly, doesn't imply automation exists. (4) Redis/minio profiles are provisioned but genuinely unwired (no code reads `REDIS_URL`; no file-storage abstraction exists yet).
- [x] **Gap (2) [icon/thumbnail placeholders] and the "no frontend embed" gap both closed** (2026-09-04): `server.Dockerfile` gained a `node:22-alpine` `webbuild` stage that runs `apps/web`'s real production build and copies `dist/` into the runtime image; `apps/server` now mounts it via a new `internal/platform/http.MountSPA` (a chi `NotFound` fallback serving `index.html` for any non-`/api`/`/health` GET, so client-side routes like `/sales` survive a hard refresh) — gated on `WEB_DIST_DIR` existing, so `go run`/`-migrate`/local dev are unaffected. Verified for real: built the SPA (`npm run build`), ran the actual `apps/server` binary against a real Postgres pointed at that `dist/` directory (no vite dev server, no proxy), and confirmed `GET /`, `GET /sales` (SPA fallback), a real `/assets/*.js` file, `/health/live`, and `/api/v1/auth/login` all return the right status codes — this was previously undetected because Stage 10b-1/10b-2's own frontend verification (line above) ran against `vite`/its dev proxy, never against the actual Docker image, so the packaging gap survived two frontend stages unnoticed. `deploy/casaos/icon.png`/`thumbnail.png` are real generated assets now (simple, not a designer's final pass — see `apps/web/src/components/Logo.tsx` for the same mark), replacing the placeholder jsdelivr URLs that used to 404; the manifest's "backend-only, no web interface yet" description text is corrected.
- [x] `docs/operations/deployment.md`: real deployment doc — actual commands, the two-role reasoning, profile-by-profile explanation, the "no external connection required for core operation" claim made concrete, gaps stated plainly

## Stage 10b-1 — Frontend Foundation ✅ (2026-09-03)
- [x] `apps/web` scaffolded: Vite + React 19 + TypeScript strict mode, TanStack Query + Router (code-based routing, see `src/router.tsx`'s comment for why — a route-factory function's plain-`string` parameter was silently widening literal path types out of the router's type-checked union; fixed with a generic, documented in place as a real gotcha for future route additions), React Hook Form + Zod, Apache ECharts (`echarts-for-react`)
- [x] Design system: CSS custom-property tokens (light/dark, both an explicit toggle and `prefers-color-scheme`), self-hosted fonts via `@fontsource` (Space Grotesk / IBM Plex Sans / IBM Plex Mono — no runtime call to Google Fonts or any external host, consistent with "no external connection required"), tabular numerals on every figure, WCAG-conscious (visible focus rings, labeled fields, `prefers-reduced-motion` respected)
- [x] Design direction (via the `frontend-design` skill): "a working ledger, not a consumer dashboard" — cool paper (not the cliché warm-cream default), a single deep-emerald accent, a "ledger stub" left-accent-bar stat card as the one signature element — verified by real screenshot, not just described
- [x] Auth flow against real Stage 2 endpoints: bootstrap (first-run org+owner setup), login (incl. MFA-required challenge), logout — session lives entirely in the backend's HttpOnly cookie, frontend only keeps a non-secret `{orgId, userId}` UI hint
- [x] App shell: primary nav (Dashboard/Sales/Purchases/Inventory/Contacts/Accounting/GST-Tax/Reports/Integrations/Settings), global search + quick-create placeholders, theme toggle, user menu — responsive 375/768/1440px, verified by real screenshots at all three
- [x] Dashboard wired to the real Stage 7 `/reports/dashboard` + `/reports/sales/summary` endpoints — all 8 brief §23 cards, one ECharts trend chart, real loading/empty/error states (an empty freshly-bootstrapped org renders correctly, not broken)
- [x] Verified end-to-end via Playwright against a real backend (Stage 10a docker-compose): bootstrapped a fresh org and logged in through the actual UI (not just unit-tested components), confirmed real non-mocked ₹0.00 data, dark mode, and all three viewports
- [x] **Real bug found and fixed via the 375px screenshot, not assumed correct from the CSS**: a classic CSS Grid `1fr`-track content-overflow issue pushed the mobile bottom nav below the fold instead of keeping it pinned/reachable — fixed with `height: 100%` (not `min-height`) on the shell + `min-height: 0` on the scrolling grid areas
- [x] TypeScript strict mode: zero errors (strict mode had to be added explicitly — the Vite template didn't turn it on by default, a real gap that would've silently sat there). Lint (`oxlint`, the template's shipped linter): zero errors, 3 known/accepted fast-refresh warnings on the two context-provider files
- [ ] **Not built yet, left honestly for a later pass**: sales/purchases/inventory/contacts/accounting/gst/reports/integrations/settings screens are placeholders (their backend APIs are all already built and tested); global search and quick-create are visual placeholders with no wired behavior; bundle size warning from the production build (1.56MB main chunk, mostly ECharts) — code-splitting not addressed in this foundation pass

## Stage 10b-2 — Frontend Feature Screens ✅ mostly (2026-09-03, real gaps below)
- [x] Sales screen — brief §48's "exceptional attention" screen: barcode/keyboard-driven billing (`BillingPage`), customer balance/credit-limit visibility, stock/price/tax visibility, hold/restore draft, print-after-finalize, sales list + detail views
- [x] Inventory, purchases, contacts, catalogue, accounting, GST/tax, reports screens wired to their already-built and tested backend APIs
- [x] Free-first e-Way Bill UX (backend built in Stage 8c, `docs/architecture.md` §9b): invoice-screen `EwayBillCard` (Ready/Needs Information/Waiting-for-portal/Complete states), Settings → GST → E-Way Bill mode radio (Free Government Portal default, Automatic API alternate), the "portal assistant" flow (prepare → open portal in a new unproxied tab → popup-blocked fallback link → manual result entry), vehicle/transporter master sections — plain language throughout, no "JSON"/"API" shown to the user
- [x] Global search (combined customer+product, debounced) and quick-create menu in `AppShell`, wired to real endpoints
- [x] Code-splitting: every route lazy-loaded via `lazyPages.ts` (`router.tsx`'s own comment documents why — the pre-split build had a single >1.5MB chunk); `DashboardPage`'s own chunk is still ~1.1MB because it pulls in the full `echarts-for-react` eagerly — deferring an ECharts-specific slim-import pass as a real, known follow-up, not silently accepted as fine
- [x] **Accessibility pass (WCAG 2.2 AA), real issues found and fixed, not just reviewed**: three light-mode color-token pairs failed the 4.5:1 text contrast minimum (`--color-text-faint` 2.83:1, `--color-positive` 3.82:1, `--color-warning` 4.18:1 — computed by script, not eyeballed) and were darkened to ≥4.6:1 (dark-mode tokens already passed); three inputs (`ContactsPage`/`CataloguePage` search, already-fine ones left alone) had only a placeholder as their name, no label — added `aria-label`; two dropdown result lists used `role="listbox"` over `<li><button>` children, which violates ARIA's required-owned-elements rule (listbox needs `option` children) — changed to the `role="menu"`/`role="menuitem"` pattern already used correctly elsewhere in the same files; no popup (`AppShell`'s user/create/search menus, `BillingPage`'s customer dropdown) closed on Escape — added; added a skip-to-main-content link (`AppShell`, WCAG 2.4.1) and `aria-current="page"` on the active nav item; added `scope="col"` to every static `<th>` across 8 files; the dashboard's ECharts trend line had zero text alternative for a screen-reader user (WCAG 1.1.1, canvas-rendered) — added a visually-hidden `<table>` twin carrying the same day/total data and marked the chart `aria-hidden`. Verified via a real `npm run build` (TS strict, zero errors) + `npm run lint` (same 3 pre-existing, accepted fast-refresh warnings, zero new ones) after every change — not just reasoned about.
- [ ] **Not built, explicitly deferred** (unchanged from the original plan, not silently dropped): a dedicated pending-tasks center for unfinished e-Way Bills and a bulk-prepare queue (the `SplitBatch` backend primitive exists per Stage 8c but has no UI); e-Way Bill first-time-use explainer; result-import via file/PDF (manual entry only); full keyboard arrow-key navigation inside the search/customer dropdown menus (Tab reaches every item, but there's no roving-tabindex/arrow-key support yet — a real usability gap for power users, not a hard WCAG failure since the items remain keyboard-operable via Tab)
- [ ] `apps/desktop`: Tauri 2 thin shell around the same web build, zero business logic duplicated — not started, needs a Rust toolchain this pass didn't have
- [ ] Windows packaging prep (MSIX path documented, not signed/submitted yet)

## Stage 11 — Hardening
- [ ] **Full security review: IDOR, cross-tenant, RBAC bypass, CSRF, XSS, SQLi, mass assignment, rate-limit bypass, session fixation, webhook replay, API-key scope bypass, MCP permission bypass** (brief §69). **Substantial further progress this pass (2026-09-04, real Docker/Postgres now available), still not the full item:**
  - **Cross-tenant / `organisation_id` scoping (ADR 0006): now independently verified end-to-end, not just built.** `go test -tags=integration ./...` run for real against a live Postgres 18 container: **92/92 pass, 0 failures**, including every RLS cross-tenant test by name (`TestAccounting_RLS_BlocksCrossOrganisationJournalRead`, `TestCatalogue_RLS_BlocksCrossOrganisationProductRead`, `TestContacts_RLS_BlocksCrossOrganisationPartyRead`, `TestInventory_RLS_BlocksCrossOrganisationBalanceAndMovementReads`, `TestPricing_RLS_BlocksCrossOrganisationPriceListRead`, `TestRLS_Sweep_Stage4Tables` and others, plus `TestMCP_InvoicesScopedKey_CannotReadAnotherOrganisationsInvoice`). The one documented exception (`organisation.OrganisationRepository.GetByID`) was re-read and confirmed to be genuinely safe, not a missed gap — its sole caller always passes the principal's own org ID, never a client-supplied one.
  - **CSRF: reviewed, adequate, no code change.** Session cookie is `HttpOnly` + `SameSite=Lax` (`internal/modules/identity/httpapi/handlers.go`); CORS (`internal/platform/http/security.go`) is a strict allow-list with `Access-Control-Allow-Credentials` set only for an exact origin match, never a wildcard+credentials combination. Together these block both a plain cross-site form POST (SameSite=Lax withholds the cookie) and a credentialed cross-origin `fetch` (CORS rejects any origin not on the explicit allow-list). This is a legitimate modern posture, not a gap requiring a traditional CSRF token.
  - **Session fixation: reviewed, not vulnerable, no code change.** `identity/app.Service.Login` always mints a brand-new random session token + UUIDv7 session ID on every successful login (`internal/modules/identity/app/service.go`); nothing resembling "accept a client-supplied pre-auth session ID" exists.
  - **Rate-limit bypass: reviewed, correctly scoped, no code change.** `internal/modules/identity/app/ratelimit.go` is a per-email (not per-IP) exponential-backoff limiter — the right key for the actual threat (protecting one account from brute force) and not trivially bypassed by rotating IPs. Its own comment already honestly documents the real, known limitation (in-process state, needs a shared store once this deployment target moves past a single replica) — matches `docs/architecture.md §16`'s actual single-replica target, so not a live gap for this deployment.
  - **Webhook replay: reviewed, already correct, no code change.** `internal/modules/webhooks/app/service.go`'s `Sign` computes HMAC-SHA256 over `timestamp + "." + eventID + "." + body` and sends `X-Webhook-Timestamp` — a receiver can and is told to enforce a reasonable timestamp window, which is the real defense against replaying a captured payload. Signature-only would have been the gap; this isn't that.
  - **MCP permission bypass: reviewed, already correct, no code change.** `apps/mcp/main.go`'s own comment ("goes through the exact same `permissions.Checker.Require` path") checks out by reading `internal/modules/ai/tools.go`: every tool call runs through `scopedCtx` → `permissions.WithAPIKeyScopeRestriction`, and the entire toolset is read-only by construction — there is no create/update/delete tool at all, not just a documented convention not to add one.
  - **XSS: reviewed, low/no exposure, no code change.** No `dangerouslySetInnerHTML` anywhere in `apps/web/src`; the one `template`-related backend hit (`internal/modules/sales/printing`) is a PDF-layout term, not Go's `html/template` — printing draws directly onto a PDF canvas, not through HTML interpolation. React's default escaping covers the rest.
  - **Still NOT reviewed this pass** (genuinely open, not silently dropped): IDOR beyond the organisation-scoping already covered, RBAC bypass, mass assignment, API-key scope bypass (distinct from the MCP-specific check above — the general API-key-issued-scopes path across every module needs its own pass).
- [x] **Load/performance test at 100k products / 1M invoice lines, `EXPLAIN ANALYZE`-driven indexing — done for real, not simulated.** Seeded a real local instance (Postgres 18 via `docker run`, `go run ./apps/server -migrate`, real `POST /auth/bootstrap` + `POST /auth/login`) to the exact target scale: 100,000 products, 100,000 variants, 202,682 sales documents, 1,000,000 sales document lines. `scripts/loadtest/product_search.js` and `scripts/loadtest/create_sales_document.js` (k6) committed and actually run against the live API, not just written.
  - **Found and fixed (migration `0032_stage11_listing_indexes`):** `ProductRepo.ListByOrganisation`'s `ORDER BY name` had no supporting index, forcing a disk-spilling external merge sort (11.9MB temp file) — 357ms → 27ms (13x) after adding `(organisation_id, name)`. Same class of gap existed on `parties` (`ORDER BY legal_name`) and `sales_documents` (`ORDER BY issue_date` — its existing index isn't composite with `organisation_id`, so its cost scales with every organisation's rows combined once this deployment has more than one tenant, not just today's). All three are pure additive index migrations verified via `EXPLAIN ANALYZE` before and after — no code or API change.
  - **Found and honestly NOT fixed — a real but cross-cutting, breaking-change-risk gap, documented rather than guessed at:** `GET /catalogue/products` with no `q` has zero pagination — confirmed at scale: **100,000 rows = a 36.8MB single JSON response**, 270ms on localhost but a genuine unbounded-payload risk over a real network or from a memory-constrained client. Fixing this properly (cursor/offset pagination) changes the endpoint's response shape and needs a matching `apps/web` change — out of scope to guess at in this pass.
  - **Found and honestly NOT fixed — needs a deployment-hardware-specific decision:** `DATABASE_MAX_CONNS` defaults to 20 (`internal/platform/config/config.go`). Under exactly 20 concurrent `k6` VUs hitting the (correctly indexed, correctly `LIMIT`-capped) product search endpoint, p95 latency was 731ms vs. 45ms for the same query run alone — connection-pool queueing, not a query-plan problem. The right default depends on target hardware this pass has no way to determine; flagging with real numbers rather than picking an arbitrary new default.
  - **Found and honestly NOT fixed — large, systemic, genuinely out of this pass's scope:** every module except `ewaybill` (14 of 15) serializes raw, untagged Go domain structs directly via `httpx.WriteJSON`, so most of the API actually returns **PascalCase** JSON (`DocumentNumber`, `HSNSACCode`, `OrganisationID`, ...), not the snake_case the OpenAPI spec documents and the identity module's own hand-written DTOs correctly use. **This is not a live outage** — `apps/web`'s TypeScript was written against this exact real behavior (confirmed: `PurchasesPage.tsx`, `BillingPage.tsx`, `SalesDetailPage.tsx`, `SalesListPage.tsx`, `CataloguePage.tsx`, `sales/types.ts` all read `.DocumentNumber`/`.HSNSACCode` etc.), so the shipped frontend works end-to-end as-is. But it means the OpenAPI spec is wrong for most of the API surface, and any third-party integrator or the MCP server's tool-result shape following the documented contract would get misled. Fixing this correctly requires a coordinated backend (add `json:"snake_case"` tags, or introduce a proper DTO layer like identity already has) **and** frontend change across at least 6 files — a real, valuable follow-up, not something to half-fix here.
  - **Write-path concurrency, real numbers:** `POST /sales/documents` (draft creation — numbering allocation + RLS-scoped insert, the real write path) under 10 concurrent VUs for 40s: 2,681 requests, **67 req/s, p95=19.3ms, max=113ms, 0% errors**. Healthy, no queueing observed at this concurrency (contrast with the read-path pool-saturation finding above, at double the VU count).
- [ ] Concurrency scenarios re-verified at realistic scale — **partially covered** by the write-path load test above (sustained concurrent writes, zero errors, no numbering collisions observed), but not the full multi-scenario sweep (e.g., adversarial concurrent-finalize races) the original item means. Still open.
- [x] **Accessibility review (WCAG 2.2 AA) on `apps/web` — done for real, not just reviewed.** Automated audit (axe-core via Puppeteer, real login through the actual `/login` form, real authenticated navigation — not a static HTML check) against `apps/server`'s own SPA-embedding path (`WEB_DIST_DIR`, the real production serving mode, not the Vite dev server). Found the same 2 real violations present on **every** authenticated screen checked (dashboard, billing, settings, contacts):
  - **Critical (`button-name`):** `AppShell`'s user-menu button (`.userButton`) had only an `aria-hidden` avatar glyph inside it — zero accessible name. Fixed: added `aria-label="User menu"`.
  - **Serious (`color-contrast`):** the sidebar's "NodeDR Infotech Private Limited" footer text measured 4.22:1 against the sidebar's actual dark-mode background (`--color-surface: #171f1c`) — under the 4.5:1 minimum. This is the *same* `--color-text-faint` token Stage 10b-2 already darkened once, but that earlier pass evidently wasn't checked against this specific sidebar background. Fixed: darkened dark-mode `--color-text-faint` from `#71847a` to `#7a8d83` (4.22:1 → 4.78:1), in both the `prefers-color-scheme` and explicit `data-theme="dark"` definitions in `src/styles/tokens.css`.
  - Re-audited after the fix: **0 violations** on dashboard/settings/contacts. `npx tsc -b` and `npm run lint` both clean — 0 new issues, the same 3 pre-existing accepted fast-refresh warnings as before.
  - Not covered this pass: sales-list and a couple of other routes hit a test-harness navigation-timing flake (client-side route transition not settling within the audit script's wait window) rather than a real app issue — the same `BillingPage.tsx` route that flakily failed a "missing `<h1>`" check on one run has an unconditional `<h1>` in its source, confirming the flake. A Playwright-based a11y sweep (reusing the harness Stage 10b-1/10b-2 already built) would be more resilient than this pass's ad hoc Puppeteer script for a future, fuller run.
- [x] **Backup/restore test — restore into clean environment, verify reconciliation (Scenario N) — done for real.** `pg_dump -F c` the seeded instance (100,000 products / 202,682 sales documents / 1,000,000 sales document lines / 57 RLS policies) → `pg_restore` into a brand-new, empty database → row counts for all four tables matched **exactly** and **all 57 RLS policies were present and identical** post-restore. Not a schema-only smoke test — real data, real policy count, real diff.
- [x] **Migration test against a copy of prior schema — done for real.** `go run ./apps/server -migrate` against a completely fresh Postgres 18 container (via the real `deploy/compose/postgres-init/01-create-runtime-role.sh` first-boot script, not a shortcut) applied all 32 migrations cleanly, twice in this pass (once for the initial seed, again after adding migration `0032`) — both times with a clean exit and no manual intervention.
- [ ] All Scenarios A–N passing end-to-end in one continuous run — not attempted this pass, genuinely open.

## Stage 12 — Invoice Branding + Frontend Field Audit ✅ (2026-09-07)
New requirement, not in the original brief: a page-by-page read of every real `apps/web` screen against the tested backend behind it (full findings: an internally-shared audit artifact, not duplicated here) found that `internal/modules/sales/printing`'s templates have supported a logo, address, phone/email/bank details since Stage 5b, but `sales/app.Service.BuildInvoiceData` only ever populated `Seller.LegalName`/`GSTIN` — every invoice ever printed rendered a bare header no matter what the template could show, and Settings had no screen to set any of it. This stage closes that specific gap plus four smaller UI-only ones found in the same pass.
- [x] `migrations/0034`: additive `legal_entities` columns (phone, email, website, address, bank_name/account/ifsc, upi_id, authorized_signatory_name, default_terms_and_conditions, logo_png) — nullable, same precedent as `0017`'s gstin/gst_state_code
- [x] `organisation.LegalEntityRepository.UpdateInvoiceBranding` + `InvoiceBrandingUpdate` (a struct, not 10+ positional args) — logo has real three-way "replace / clear / leave unchanged" semantics (`RemoveLogo` distinct from an absent upload), proven by `TestSales_Print_UsesLegalEntityInvoiceBranding` doing all three against real Postgres, not just the additive case
- [x] `PUT /legal-entities/{id}/invoice-branding` — logo arrives as base64 in the JSON body (`logo_png_base64`), decoded via `image.Decode` and **re-encoded to PNG server-side regardless of upload format** (`decodeAndReencodeLogo`) — this IS the validation (a malformed file fails to decode, never gets stored), same "don't blindly trust an uploaded image" principle the brief already applies to OCR uploads elsewhere. Max 2MB / 1000×1000px, enforced before decode.
- [x] `sales/app.Service.BuildInvoiceData` now actually reads the legal entity's branding into `printing.SellerInfo`/`InvoiceData` — logo drawn top-left (skipped on thermal templates, no room), phone/email/website header line, UPI ID as text next to the bank block (a scannable QR needs a QR-encoding dependency this package doesn't have yet — real, deliberate follow-up, not attempted here), authorized signatory name under the signature line. A document's own `TermsAndConditions` (set at billing time) still wins; the legal entity's `DefaultTermsAndConditions` is only the fallback for a document that set none — never overwrites an explicit per-invoice value.
- [x] `apps/web` Settings gained an "Invoice branding" panel — logo upload with live preview/remove, all the fields above, same remount-on-save-success key pattern the existing GST-details form already uses
- [x] Verified for real, not just written: full `go build ./...`, `go vet ./...`, `gofmt -l` clean; the ENTIRE existing test suite (unit + `-tags=integration` against a real Postgres 18 container via testcontainers) re-run and passing, 0 regressions; the new integration test specifically exercises the tricky "leave logo unchanged on an unrelated field update" branch, not just the happy path; a full HTTP-level smoke test (bootstrap → login → `PUT`/`GET /legal-entities/.../invoice-branding` with a real generated PNG) run against a live `go run ./apps/server` + fresh Postgres to confirm the exact JSON shape the frontend sends matches what the handler expects — invalid-base64 logo correctly rejected with `INVALID_LOGO`, no-op update correctly preserves the existing logo, `remove_logo` correctly clears it. `apps/web`: `tsc -b`, `oxlint` (same 4 pre-existing accepted fast-refresh warnings, 0 new), `npm run build` all clean. No browser extension available in this environment to click through the React form itself — that specific gap is flagged, not silently assumed fine.
- [x] Four smaller fixes from the same audit pass, unrelated to branding: sidebar/auth-screen "NodeDR Infotech Private Limited" credit is now a real link to nodedr.com; every `ReportTable` (Sales, Purchases, Inventory, Accounting, GST, Reports) gained CSV/Excel/PDF export buttons on top of the `?format=` support `internal/platform/export` already had since Stage 7 but no screen ever exposed, plus real per-report download filenames instead of a hardcoded `report.csv` for every export; GST page gained a Tax-rate summary panel (endpoint existed, was unused); invoice detail page now shows the customer's name and a `wa.me` "Share via WhatsApp" deep link (text-only — the anonymous share-link redeem endpoint from Stage 9 doesn't stream document bytes yet, a real, separate follow-up, not worked around here)
- [ ] **Not built, explicitly deferred**: a scannable UPI payment QR (text-only for now); Integrations page is still the one bare placeholder in the whole nav (API keys + webhooks are fully built server-side, Stage 9, with no screen). See the shared audit artifact for the full prioritized list.

## Stage 13 — Contacts Detail, Inventory Stock Card, Purchases A11y ✅ (2026-09-07)
Continuation of Stage 12's same audit-driven pass — the three next-highest items from its "Now" lane, all closing the identical pattern (a real, tested backend endpoint with zero frontend caller).
- [x] `apps/web` gained `/contacts/$id`: profile header, a ledger + ageing view (`GET /accounting/parties/{id}/ledger`/`ageing` — built and tested since Stage 6, never called from any screen), a GSTIN/state add-registration form, an address add-form, and a record-payment form (`POST /accounting/receipts`/`payments`, same story — Stage 6, never wired). Contacts list rows are now real links with a customer/supplier filter (a BOTH party matches either).
- [x] Inventory replaced its "two report tables and nothing else" with a real per-product stock card: search (reuses `sales.BillingLookup`, the same endpoint the billing counter already trusts), current/reserved/available/average-cost, a movement timeline (`GET /inventory/movements`), and a manual adjustment form (`POST /inventory/adjustments`, movement types ADJUSTMENT_IN/OUT/DAMAGE/EXPIRY) — the report tables stay, now supplemented rather than being the whole screen.
- [x] Purchases list: fixed a real accessibility bug (bare `<tr onClick>`, no keyboard access, no role) and added the supplier name column (previously an unresolved id only visible in network traffic, not the UI).
- [x] Verified against a real Postgres 18 + live `go run ./apps/server` for every new call path (create-party, add-tax-registration, add-address, record-receipt, read-ledger/ageing, billing-lookup, get-balance, record-adjustment, list-movements) — each JSON shape hand-confirmed against the frontend's own TypeScript interfaces before committing, not assumed. `apps/web`: `tsc -b`/`oxlint`/`npm run build` clean, no backend Go changes this stage.

## Cross-cutting (touched every stage, not a separate phase)
- [x] `docs/api.md`, `docs/database.md` (2026-09-03) — written from the actual migrations/handlers, not from architecture-doc intent; `docs/database.md` cross-checked against a `CREATE TABLE` grep across all 31 migrations (64 tables), `docs/api.md`'s module→base-path table verified against each module's real `Mount()`
- [x] OpenAPI 3.1 spec (2026-09-03) — `api/openapi/openapi.yaml`, all 124 real routes across all 15 HTTP modules, mechanically verified complete via a grep-and-diff of every `r.Get/Post/Put/Delete/Patch(...)` registration against the spec's paths (exact match). Real bug caught in the process, not just documented: `export.WriteJSON` lost column order and dropped the `headers` field (Go map key sorting) — every `ReportTable`-based screen and the dashboard's sales trend chart would have broken or shown wrong columns the first time real data existed; fixed with a regression test. Keeping this current as endpoints change is still a live, ongoing obligation — this entry marks the spec as caught up as of this commit, not as permanently done
- [x] `CHANGELOG.md` (2026-09-03) — derived from real git history, one entry per shipped stage
- [ ] ADRs for every non-obvious decision — 5 exist (`docs/adr/`); not audited for full coverage against every non-obvious decision actually made
- [x] CI workflow written (2026-09-03) — `.github/workflows/ci.yml`: gofmt/vet/staticcheck/govulncheck, unit tests, integration tests (Testcontainers), frontend typecheck/lint/build, docker build, compose config (brief §73). Syntactically validated (parses as valid YAML, job/step structure checked by hand against each real command); **not yet run for real** — this environment has no GitHub Actions runner, Docker, or Postgres to exercise it against. Confirm the first real PR run is green before trusting this checkbox fully

## Open questions — answered 2026-09-03, see `docs/research.md`
- [x] e-Invoice/e-Way Bill provider: **staying on the NIC sandbox** (user decision) — no real GSP/ASP account yet, matches how the whole project has been built and tested
- [x] First deployment target: **fresh VPS** (user decision) — the plain `deploy/compose/` path is the one to harden/finish first; `deploy/casaos/` stays as-is, not the near-term priority
- [x] Final product/branding name: **Rechvix** (decided and applied 2026-09-04) — GitHub repo, Go module path, and all docker/workflow/doc references renamed from `billing-platform` accordingly
